/* Fond viking mystique */
html, body {
  height: 100%;
  margin: 0;
  padding: 0;
}

body {
  font-family: 'HYWenHei-85W', sans-serif;
  background: url('https://stock.adobe.com/fr/images/scandinavian-fleet-sailing-on-long-wooden-ships-on-mysterious-waters-fantasy-landscape-with-vikings-on-dragon-boats-with-shields-and-sails-in-a-dark-sea-in-a-mist-among-sharp-rocks-and-mermaids/487986227?prev_url=detail') no-repeat center center fixed;
  background-size: cover;
  color: #ecf0f1;
  text-align: center;
  display: flex;
  justify-content: center;
  align-items: center;
}

/* Conteneur du jeu */
#game {
  background: rgba(0, 0, 0, 0.6);
  padding: 30px;
  border-radius: 15px;
  width: 90%;
  max-width: 700px;
  box-shadow: 0 0 20px rgba(0, 0, 0, 0.8);
  transition: transform 0.3s;
}

#game:hover {
  transform: scale(1.02);
}

/* Texte */
#text {
  min-height: 80px;
  font-size: 18px;
  transition: opacity 0.5s;
}

/* Boutons */
button.choice {
  margin: 10px;
  padding: 12px 20px;
  border: none;
  border-radius: 10px;
  cursor: pointer;
  background: #16a085;
  color: white;
  font-size: 18px;
  font-family: 'HYWenHei-85W', sans-serif;
  transition: background 0.3s, transform 0.2s;
}

button.choice:hover {
  background: #1abc9c;
  transform: scale(1.05);
}

/* Image personnage */
.character {
  width: 180px;
  border-radius: 50%;
  margin-bottom: 15px;
  transition: opacity 0.5s, transform 0.3s;
}

.character:hover {
  transform: scale(1.1);
}

/* Contrôles musique */
#music-controls {
  position: fixed;
  bottom: 20px;
  right: 20px;
  z-index: 1000;
  display: flex;
  gap: 10px;
}

#music-controls button {
  padding: 10px;
  background-color: #16a085;
  color: white;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  font-size: 16px;
  transition: background-color 0.3s;
}

#music-controls button:hover {
  background-color: #1abc9c;
}

#music-controls input[type=range] {
  cursor: pointer;
}
